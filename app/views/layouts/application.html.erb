<!DOCTYPE html>
<html>
  <head>
    <title>My Life</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if user_signed_in? %>
    <header>
      <div>
        <%= link_to image_tag("logo.png", alt:"My Life", style:"width:130px; height:auto;"),root_path, class: "text-white nav-link" %>
      </div>
      <div><%= link_to "トップ", events_path, class:"text-white nav-link" %></div>
      <div><%= link_to "出来事追加", new_event_path, class:"text-white nav-link" %></div>
      <div><%= link_to "グラフ", graphs_path, class:"text-white nav-link" %></div>
      <div><%= link_to "ユーザ一覧", users_path, class:"text-white nav-link" %></div>
      <div><%= link_to "マイページ", user_path(current_user.id), class:"text-white nav-link" %></div>
      <div><%= link_to "ログアウト", destroy_user_session_path, data:{ turbo_method: :delete }, class: "text-white nav-link" %></div>
    </header>
    <% end %>
    <%= yield %>
  </body>
</html>
